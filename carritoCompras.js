let carritoCompras = ["Elemento1", "Elemento2", "Elemento3"]

const agregarElemento = (array, elemento) => {
    array.push(elemento)
    console.log("El elemento " + elemento + " se ha eliminado del carrito")
}